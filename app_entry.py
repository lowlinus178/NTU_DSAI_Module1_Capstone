import streamlit as st

# Lightweight header so Render sees a healthy service immediately
st.set_page_config(page_title="App Entry", layout="centered")
st.title("Loading dashboard…")

# Import your full vetted app (unchanged)
import ntu_dsai_module_1_capstone_app